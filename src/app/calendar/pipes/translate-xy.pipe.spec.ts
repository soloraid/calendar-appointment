import { TranslateYPipe } from './translate-y.pipe';

describe('TranslateXYPipe', () => {
  it('create an instance', () => {
    const pipe = new TranslateYPipe();
    expect(pipe).toBeTruthy();
  });
});
